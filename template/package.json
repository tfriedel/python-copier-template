{
  "name": "{{package_name}}-probity",
  "private": true,
  "type": "module",
  "description": "Local Node deps for Probity (AI agent process discipline). Not part of the Python package.",
  "devDependencies": {
    "@nizos/probity": "^1.3.0",
    "@ast-grep/lang-python": "^0.0.6",
    "@anthropic-ai/claude-agent-sdk": "^0.2.0"
  }
}
